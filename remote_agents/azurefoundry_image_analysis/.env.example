# Azure AI Foundry Configuration
# Copy this file to .env and fill in your actual values

# Required: Azure AI Foundry Project Endpoint
# Example: https://your-project.cognitiveservices.azure.com/
AZURE_AI_FOUNDRY_PROJECT_ENDPOINT=""  #This is the only important key, found in the azure AI foundry Portal overview under Azure AI Foundry project endpoint

# Required: Azure AI Agent Model Deployment Name  
AZURE_AI_AGENT_MODEL_DEPLOYMENT_NAME="gpt-4o" #must have a model deployed, found in Azure AI Portal under 'Models + endpoints'

OPENAI_API_KEY="" #this agent needs openai gpt-image-1 model

# Hostname the agent binds to (leave blank to fall back to localhost)
A2A_ENDPOINT=localhost

# Port for the agent's A2A API
A2A_PORT=9066

# Host agent URL for self-registration (empty string disables)
A2A_HOST=http://localhost:12000

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
