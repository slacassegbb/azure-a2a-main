# ============================================================================
# Azure AI Foundry A2A Agent Template - Environment Configuration
# ============================================================================
#
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!
#

# ----------------------------------------------------------------------------
# REQUIRED: Azure AI Foundry Configuration
# ----------------------------------------------------------------------------

# Your Azure AI Foundry project endpoint
# Find this in: Azure Portal > Your AI Foundry Project > Overview > Endpoint
AZURE_AI_FOUNDRY_PROJECT_ENDPOINT=https://your-project.cognitiveservices.azure.com/

# Your model deployment name (usually gpt-4o or gpt-4)
# Find this in: Azure AI Foundry Portal > Deployments
AZURE_AI_AGENT_MODEL_DEPLOYMENT_NAME=gpt-4o

# ----------------------------------------------------------------------------
# OPTIONAL: Network Configuration
# ----------------------------------------------------------------------------

# The port your agent will listen on for A2A protocol requests
# Default: 9020 (change if running multiple agents on same machine)
A2A_PORT=9020

# The host/IP your agent advertises (for self-registration)
# Use localhost for local development
# Use your public IP or domain for production deployments
A2A_ENDPOINT=localhost

# ----------------------------------------------------------------------------
# OPTIONAL: Host Agent Registration
# ----------------------------------------------------------------------------

# URL of the host orchestrator agent for automatic registration
# Leave blank to disable auto-registration
# Default: http://localhost:12000 (standard host agent port)
HOST_AGENT_URL=http://localhost:12000

# ----------------------------------------------------------------------------
# OPTIONAL: Logging Configuration
# ----------------------------------------------------------------------------

# Enable verbose debug logging (true/false)
# Default: false (set to true for troubleshooting)
VERBOSE_LOGGING=false

# ----------------------------------------------------------------------------
# OPTIONAL: Azure Authentication
# ----------------------------------------------------------------------------

# If using Service Principal authentication instead of DefaultAzureCredential:
# (Usually not needed for local development with az login)

# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# AZURE_TENANT_ID=your-tenant-id

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
#
# 1. DefaultAzureCredential is used by default, which tries multiple auth methods:
#    - Environment variables (above)
#    - Managed Identity (in Azure)
#    - Azure CLI (az login)
#    - Visual Studio Code
#    - Azure PowerShell
#
# 2. For local development, run 'az login' before starting your agent
#
# 3. Ensure your Azure AI Foundry deployment has sufficient quota:
#    - Minimum 20,000 TPM (Tokens Per Minute) recommended
#    - See foundry_agent.py for detailed quota information
#
# 4. Bing search is optional and configured in Azure AI Foundry Portal
#    - If not configured, agent will work without web search
#    - Add a connection named "agentbing" in your AI Foundry project
#
# 5. For production deployment:
#    - Set A2A_ENDPOINT to your public domain/IP
#    - Use Azure Key Vault for secrets
#    - Enable Application Insights for monitoring
#
